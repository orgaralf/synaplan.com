This directory is only needed for some tool features, where we stream audio to a local whisper service.
Most of the app runs without it or you can also use Groq or Google for audio transcription, 
which works faster and better than most local solutions.